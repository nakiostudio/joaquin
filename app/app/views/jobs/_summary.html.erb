<div class="panel panel-default">
  <div class="panel-heading"><strong><%= t('jobs.summary.title') %></strong></div>
  <div class="panel-body">
    <% if @jobs.empty? %>
      <span class="label label-danger"><%= t('jobs.summary.placeholder') %></span>
    <% else %>
      Panel content
    <% end %>
  </div>
</div>
